# Copy to .env and fill in values — .env is gitignored
#
# Node that holds the db-nvme-storage PV for atuin.
# Find it after applying atuin_pvc.yaml:
#   kubectl get pv -l storageclass=db-nvme-storage -o wide
ATUIN_NODE_NAME=
